# Copy to downstream-repos.local.toml (gitignored) to record private downstream
# consumers without naming them in the tracked, public downstream-repos.toml.

[downstream_repos]
tracking_basis = "explicit test or dependency integrations (private consumers)"
paths = [
  "./health-repos/example-private-consumer",  # conftest.py + pyproject.toml
]
